[project]
name = "firmware"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "eth-account>=0.13",
    "pygame>=2.6.1",
    "qrcode>=8",
]
