window.DOCS=[{"u":"index.html","p":"Overview","t":"TICK SDK","a":"tick-sdk","x":"Build a crypto arcade game for the TICK handheld in one file. The device is a 480\u00d7320 landscape screen, a rotary dial, two buttons and a speaker. A game on it is always the same shape: **a claim about where a price will be at a known moment, backed by money**. This SDK owns everything around that claim \u2014 the live price, the clock, the odds, the wallet, the settlement, the input and the noise \u2014 so your game only has to say what a bet *is*, what wins, and what it looks like. from tick import Game, run from tick.hud import clear, draw_clock, draw_hud from tick.ui import YELLOW, say class Coinflip"},{"u":"index.html","p":"Overview","t":"Install","a":"install","x":"cd sdk python3 -m venv .venv && source .venv/bin/activate pip install -e . # demo play: only pygame pip install -e \".[chain]\" # + real USDC settlement and ENS names pip install -e \".[pi]\" # + the dial and the panel buttons tick doctor # what is installed, and whether the feed answers Python 3.11 or newer."},{"u":"index.html","p":"Overview","t":"Sixty seconds","a":"sixty-seconds","x":"tick new mygame # scaffold a playable game python mygame.py # play it on simulated prices tick prices --source coinbase --limit 10 # watch a live feed in the terminal tick run mygame.py --source coinbase # play it on real prices"},{"u":"index.html","p":"Overview","t":"What you get","a":"what-you-get","x":"| Layer | Module | What it owns | |---|---|---| | Ticks | `tick.feeds` | one validated `PriceTick`; simulated, exchange, on-chain and recorded feeds | | Market | `tick.market` | history, realized volatility, fresh / quiet / ready, probability of a range | | Odds | `tick.pricing` | chance \u2192 fair multiple, with a cap, a floor and a house edge you set | | Money | `tick.money` | integer micro-USDC wallet; nothing here is a float | | Settlement | `tick.escrow` | deposits, sessions and payouts on-chain, on a worker thread | | Identity | `tick.identity` | a readable name per wallet, and a leaderboard"},{"u":"index.html","p":"Overview","t":"Documentation","a":"documentation","x":"All of it is also a **browsable site** with search, built from these same Markdown files \u2014 open [`web/dist/index.html`](web/dist/index.html) straight from disk, or: tick docs # build it and open a browser python web/build.py # build only, into web/dist/ * **[PLAN.md](PLAN.md)** \u2014 the system from first principles, with examples, and the increment-by-increment plan to build or extend it. * [docs/writing-a-game.md](docs/writing-a-game.md) \u2014 the tutorial, start here. * [docs/prices-substreams.md](docs/prices-substreams.md) \u2014 putting an on-chain price under your game: Substreams, the relay, and how"},{"u":"index.html","p":"Overview","t":"The command line","a":"the-command-line","x":"tick doctor what is installed, configured and reachable tick new mygame scaffold a game tick run mygame.py [--source ...] play it tick prices [--source coinbase] print the feed tick pools [-v] print every pool in the on-chain stream tick record -o eth.ndjson --seconds 300 save a real market tick replay eth.ndjson --speed 10 play it back tick board the leaderboard, read from ENS tick docs [--port 8000] build the documentation site and open it"},{"u":"index.html","p":"Overview","t":"Testing a game","a":"testing-a-game","x":"Games that hold money need tests, and a game loop is testable only if it can run without a window. `tick.testing.Harness` runs your real game class against a real market on a clock you control: from tick.testing import Harness from mygame import MyGame def test_a_wide_bet_is_never_sold(): h = Harness(MyGame(), seed=7).warm(25) assert h.ctx.bet(h.price * 0.5, h.price * 1.5) is None assert h.ctx.refused == 'TOO SURE TO SELL' SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python -m unittest discover -s tests -t ."},{"u":"index.html","p":"Overview","t":"Settings","a":"settings","x":"Everything is configured by environment, read from a `.env` beside your game (copy `.env.example`). Anything set in the shell wins, so a one-off run is `TICK_MARKET_SOURCE=coinbase python mygame.py`. | Setting | Values | |---|---| | `TICK_MARKET_SOURCE` | `sim` \u00b7 `coinbase` \u00b7 `substreams` \u00b7 `replay` \u00b7 `frozen` | | `TICK_ASSET` | `eth` \u00b7 `btc` \u00b7 `sol` \u00b7 `hbar` | | `TICK_FUNDING` | `demo` \u00b7 `arc-testnet` | | `TICK_STAKE_USDC` | money per bet, e.g. `10` or `0.001` | | `TICK_WINDOW_S` | round length, if the game does not fix it | | `TICK_ROTATE` | `auto` \u00b7 `0` \u00b7 `90` \u00b7 `180` \u00b7 `270` | | `TICK_SOUN"},{"u":"index.html","p":"Overview","t":"Examples","a":"examples","x":"| File | What it shows | |---|---| | [`examples/coinflip.py`](examples/coinflip.py) | the smallest real game: up or down at the bell | | [`examples/ladder.py`](examples/ladder.py) | park a box on the price \u2014 the shape most games take | | [`examples/ticker.py`](examples/ticker.py) | the data layer with no game and no pygame | | [`examples/pools.py`](examples/pools.py) | every pool in the on-chain stream, block by block |"},{"u":"plan.html","p":"The plan","t":"The plan","a":"the-plan","x":"What this SDK is, why each piece exists, and the order to build or extend it in. Written to be read top to bottom in about fifteen minutes. Every claim has a runnable example; every rule has the bug that taught it. ---"},{"u":"plan.html","p":"The plan","t":"Part 1 \u2014 First principles","a":"part-1-first-principles","x":"Seven questions. Answer them and the architecture falls out."},{"u":"plan.html","p":"The plan","t":"1. What is a game on this device?","a":"1-what-is-a-game-on-this-device","x":"**A claim about where a number will be at a known moment, backed by money.** That is it. A box on a price ladder, an over/under, a coin flip, a hi-lo \u2014 all the same claim with different clothes. So the SDK has exactly one bet type: a **price range** and a **round** it settles at. ctx.bet(low=2499.0, high=2501.0) # a box ctx.bet(ctx.open_price, 1e9) # \"up\": a one-sided range ctx.bet(0.0, ctx.open_price) # \"down\" Your game decides which range the player is pointing at, and what it looks like. Nothing else about a game is the SDK's business."},{"u":"plan.html","p":"The plan","t":"2. Where does the number come from?","a":"2-where-does-the-number-come-from","x":"**From somewhere real, and it must carry its own age.** A float cannot tell you when it was true. Settle money on one and you will eventually settle on a number from four minutes ago. So every feed produces a `PriceTick`: PriceTick(price=2500.12, sequence=417, received_at=10234.5, source_age=0.8) tick.age(now) # time since arrival + how old it already was `source_age` is what makes a chain block honest: a block two seconds old *is* two seconds old, and the game must know that before it settles on it. Four sources, one seam: TICK_MARKET_SOURCE=sim # seeded random walk, no network (default) TICK"},{"u":"plan.html","p":"The plan","t":"3. What is a fair price for a bet?","a":"3-what-is-a-fair-price-for-a-bet","x":"**One over the chance of winning.** Everything else is decoration. chance = market.probability(low, high, horizon) # normal CDF over the range multiple = (1 - edge) / chance # capped, floored Over a short horizon a price is a random walk in log space, and distance travelled grows with the **square root** of time, not with time. So: sigma_10s = price * sqrt(variance_per_second * 10) A one-sigma-wide band is about a 68% shot, so it pays about 1.47x. A box a long way out is a long shot and pays accordingly. Nothing is typed into a table; every number is computed from what the market is doing righ"},{"u":"plan.html","p":"The plan","t":"4. What is money?","a":"4-what-is-money","x":"**An integer.** Micro-USDC: whole units of 0.000001 USDC, the real on-chain unit. wallet.balance # 100_000_000 == 100 USDC wallet.debit(10_000_000) A balance that is a float drifts, and `0.1 + 0.2` is not a thing you want to explain to someone whose money it is. Payouts are **truncated**, never rounded: a book must not round money into existence it did not owe. >>> parse_usdc('0.57') # float('0.57') * 1e6 is 569999.9999999999 570000"},{"u":"plan.html","p":"The plan","t":"5. When does a bet settle?","a":"5-when-does-a-bet-settle","x":"**At the bell, on a price stamped at or after the bell.** The clock never stops. A round is always running; when one ends the next has already started, in the same frame \u2014 there is no ready screen, because a clock you can pause is a clock you can wait out. A price from *before* the bell cannot settle that bell, however fresh it looks. So the clock waits: bell \u2500\u2500> no post-bell price yet \u2500\u2500> SETTLING (up to 4s) \u2500\u2500> still nothing \u2500\u2500> VOID, stake refunded \u2514\u2500\u2500> price arrives \u2500\u2500\u2500\u2500\u2500> settle Refunding is the right answer when you are unsure. Settling a real bet against the wrong moment's price is wors"},{"u":"plan.html","p":"The plan","t":"6. Who is the player?","a":"6-who-is-the-player","x":"**A wallet \u2014 and a wallet is not a name.** `0x7ee8\u2026CCac` on a leaderboard is noise. So each wallet gets a readable subname (`amber-otter.tick.eth`) and its record lives in that name's own text records. The leaderboard is then *derived* rather than stored: read the registry, read each name, sort. No server, no database, nothing that dies when you stop paying for it. from tick.identity import Standings, name_of name_of('0x7ee8...') # 'amber-otter.tick.eth' Standings().read() # [Standing(name=..., pnl=Decimal('12.4'), ...), ...]"},{"u":"plan.html","p":"The plan","t":"7. What does the SDK own, and what do you?","a":"7-what-does-the-sdk-own-and-what-do-you","x":"| The SDK | You | |---|---| | the feed, its validation, its staleness | which range the player is pointing at | | volatility, probability, the multiple | how the player aims | | the round clock, settlement, voiding | what it looks like | | the wallet, the escrow, the cap | what it sounds like beyond the default cues | | dial, buttons, keys, touch, rotation | the words on the screen | | the generic cues and the music beds | | If you find yourself writing a random number generator, a volatility estimate, or a payout calculation, stop: it already exists, and the version here has the bugs already "},{"u":"plan.html","p":"The plan","t":"Part 2 \u2014 The system","a":"part-2-the-system","x":""},{"u":"plan.html","p":"The plan","t":"The shape of one frame","a":"the-shape-of-one-frame","x":"\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 feed \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba \u2502 1 market.poll() validate, fold in \u2502 \u2502 2 rounds.update() has a bell passed? \u2502 \u2502 3 book.settle() pay, refund, record \u2502 \u2502 4 cues bell, crossings, ticks \u2502 \u2502 5 your on_action() the dial and buttons \u2502 \u2502 6 your update() your state \u2502 \u2502 7 your draw() the frame \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518 The order is not arbitrary: * **Prices before the clock**, because the clock settles on a price. * **Settlement before input**, because a press arriving in the same frame as a bell belongs to the round now open, not the one that just"},{"u":"plan.html","p":"The plan","t":"The layers","a":"the-layers","x":"tick.feeds PriceTick \u00b7 SimulatedFeed \u00b7 CoinbaseFeed \u00b7 SubstreamsFeed \u00b7 ReplayFeed \u2502 PoolStream (raw, per-pool, per-block) \u25bc tick.market history \u00b7 realized volatility \u00b7 fresh / quiet / ready \u00b7 probability \u2502 \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba tick.pricing chance \u2192 multiple, with cap, floor, edge \u25bc tick.rounds RoundClock: open price, bell, settling, void \u2502 \u25bc tick.bets BetBook: place (debit) \u00b7 top up \u00b7 settle (credit) \u00b7 refund \u2502 \u25b2 \u2502 \u2502 tick.money Wallet (integer micro-USDC) \u25c4\u2500\u2500 tick.escrow (real, on-chain) \u2502 \u25bc tick.game Game + GameContext \u25c4\u2500\u2500 you write this \u2502 \u25bc tick.runtime run(): display \u00b7 input \u00b7 cues \u00b7 30 FPS \u00b7 shutd"},{"u":"plan.html","p":"The plan","t":"What a game looks like in full","a":"what-a-game-looks-like-in-full","x":"from tick import Game, run from tick.hud import Ladder, clear, draw_bet, draw_clock, draw_cursor, draw_hud class Ladder_(Game): title, window_s, arm_s = 'LADDER', 10.0, 1.5 def setup(self, ctx): self.offset = 0 # detents from the round's open price def unit(self, ctx): return max(ctx.open_price, 1.0) / 10_000 # one basis point def aim(self, ctx): centre = ctx.open_price + self.offset * self.unit(ctx) half = 4 * self.unit(ctx) return centre - half, centre + half def on_action(self, ctx, action): if action.name in ('UP', 'DOWN') and not ctx.next_bets: self.offset += 1 if action.name == 'UP' else"},{"u":"plan.html","p":"The plan","t":"Part 3 \u2014 The rules, and what each one cost","a":"part-3-the-rules-and-what-each-one-cost","x":"Every rule below is enforced in code and covered by a test. They read as paranoia until you meet the bug. | Rule | Where | What happens without it | |---|---|---| | **Stillness is a calm market, not missing data** | `Market._measure` | A flat feed was priced as \"typical ETH\". A box parked on the line won **58 bets out of 58**. | | **Nothing is sold when the price has not moved for 15s** | `Market.quiet` | On a line that never moves, a box on spot is a certain win at any multiple over 1x. | | **Nothing is sold under 1.05x** | `Game.min_multiple` | A near-certain bet is not a bet, it is a withdr"},{"u":"plan.html","p":"The plan","t":"Part 4 \u2014 Building it, one shippable increment at a time","a":"part-4-building-it-one-shippable-increment-at-a-time","x":"Each increment ends with something you can run, and a command that proves it. Nothing here depends on the increment after it."},{"u":"plan.html","p":"The plan","t":"Increment 0 \u2014 a number that arrives (half a day)","a":"increment-0-a-number-that-arrives-half-a-day","x":"*Goal: prices, validated, from more than one source.* Build `tick/ticks.py`, `tick/feeds/`. tick prices --source sim --limit 20 tick prices --source coinbase --limit 20 **Done when:** the same seed replays the same market exactly; a tick with a future timestamp, a negative price, or an out-of-order sequence is rejected rather than used."},{"u":"plan.html","p":"The plan","t":"Increment 1 \u2014 what the number means (half a day)","a":"increment-1-what-the-number-means-half-a-day","x":"*Goal: measured volatility and a probability you can defend.* Build `tick/pricing.py`, `tick/market.py`. python examples/ticker.py python -m unittest tests.test_market tests.test_pricing **Done when:** a one-sigma band prices at 68%; a dead-flat feed reports `MARKET QUIET / NO BETS` rather than a cheap certainty; one spiked print does not move the estimate by more than the cap allows."},{"u":"plan.html","p":"The plan","t":"Increment 2 \u2014 money that cannot drift (half a day)","a":"increment-2-money-that-cannot-drift-half-a-day","x":"*Goal: a wallet, in integers, with a funding seam.* Build `tick/money.py`. python -m unittest tests.test_money **Done when:** a thousand debit/credit pairs leave the balance exactly where it started, and `parse_usdc('0.57')` is `570000`."},{"u":"plan.html","p":"The plan","t":"Increment 3 \u2014 the clock and the book (one day)","a":"increment-3-the-clock-and-the-book-one-day","x":"*Goal: rounds roll on their own and bets settle honestly.* Build `tick/rounds.py`, `tick/bets.py`. python -m unittest tests.test_rounds tests.test_bets **Done when:** a missed frame does not shift the bell; a bell with no post-bell price voids and refunds after four seconds; a top-up blends the multiple."},{"u":"plan.html","p":"The plan","t":"Increment 4 \u2014 a game you can actually play (one day)","a":"increment-4-a-game-you-can-actually-play-one-day","x":"*Goal: `run(MyGame())` opens a window and plays.* Build `tick/game.py`, `tick/runtime.py`, `tick/display.py`, `tick/input.py`, `tick/ui.py`, `tick/hud.py`. tick new mygame && python mygame.py **Done when:** the dial moves the aim, A buys, the bell settles, the screen shows the balance, and Escape leaves cleanly."},{"u":"plan.html","p":"The plan","t":"Increment 5 \u2014 proof it is testable (half a day)","a":"increment-5-proof-it-is-testable-half-a-day","x":"*Goal: the rules are tested, not eyeballed.* Build `tick/testing.py` and the suite. SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python -m unittest discover -s tests -t . **Done when:** three hundred simulated rounds of a fair book drift less than a third of the money staked, and every example draws a frame without raising."},{"u":"plan.html","p":"The plan","t":"Increment 6 \u2014 a price nobody can push (one day)","a":"increment-6-a-price-nobody-can-push-one-day","x":"*Goal: the on-chain feed under the game.* Build `tick/feeds/substreams.py`; run the relay. python substreams/relay.py & tick pools -v tick run mygame.py --source substreams **Done when:** many pools across two chains become one price, an outlier pool is excluded, a chain that falls behind stops counting, and a relay that dies makes the game refuse bets rather than settle on stale blocks. See [docs/prices-substreams.md](docs/prices-substreams.md)."},{"u":"plan.html","p":"The plan","t":"Increment 7 \u2014 real money (one to two days)","a":"increment-7-real-money-one-to-two-days","x":"*Goal: a stranger funds the device from their own wallet and cashes out.* Build `tick/chain.py`, `tick/escrow.py`. python -m unittest tests.test_escrow # a fake chain: no network, no keys TICK_FUNDING=arc-testnet TICK_STAKE_USDC=0.001 python mygame.py **Done when:** a deposit opens a session in the sender's name, the balance is capped at what the escrow can pay, cash-out returns the money to the address it came from, and a restart mid-cash-out finishes the job. See [docs/money-arc.md](docs/money-arc.md)."},{"u":"plan.html","p":"The plan","t":"Increment 8 \u2014 a player people can point at (half a day)","a":"increment-8-a-player-people-can-point-at-half-a-day","x":"*Goal: names and a leaderboard.* Build `tick/identity.py`. tick board **Done when:** a wallet resolves to a name without any index, a name is trusted only when it points back at the wallet, and the board survives an unreachable RPC by keeping its last rows. See [docs/identity-ens.md](docs/identity-ens.md)."},{"u":"plan.html","p":"The plan","t":"Increment 9 \u2014 polish that is not optional","a":"increment-9-polish-that-is-not-optional","x":"Sound cues, music beds tied to what is at stake, the encoder's arming window, rotation for a portrait panel, `tick doctor`. These read as garnish and are not: the crossing swoops and the pitched countdown are how a player knows whether they are winning **without looking at the screen**, which is the entire point of a handheld you play with your thumb."},{"u":"plan.html","p":"The plan","t":"Increment 10 \u2014 documentation somebody will actually read (half a day)","a":"increment-10-documentation-somebody-will-actually-read-half-a-day","x":"*Goal: the docs are a site, not a pile of files.* Build `web/`: `mdlite.py` renders the Markdown that already exists, `build.py` assembles pages, navigation, a search index and one page per source file. tick docs python -m unittest tests.test_web **Done when:** every internal link resolves, every `#anchor` matches a real heading, search finds a rule by its words rather than its page, and the whole thing opens from `file://` with nothing installed. The Markdown stays the source of truth \u2014 a docs site with its own copy of the prose is a docs site that is wrong within a week. ---"},{"u":"plan.html","p":"The plan","t":"Part 5 \u2014 Extending it","a":"part-5-extending-it","x":"Things the seams already allow, in rough order of value: | Want | Where it goes | Notes | |---|---|---| | A new venue (Binance, a DEX aggregator) | a class in `tick/feeds/` with `poll`/`close` | register it in `open_feed` | | A new asset | `ASSETS` in `tick/ticks.py` | needs a realistic `sigma`; a wrong one teaches wrong odds | | A different bet shape (a ladder of ranges, a parlay) | your game, using several `ctx.bet` calls | the book already settles many bets per round | | A house edge | `Game.house_edge = 0.03` | and say so on screen; hiding it teaches the player the wrong thing | | Multi-ro"},{"u":"plan.html","p":"The plan","t":"What is deliberately not here","a":"what-is-deliberately-not-here","x":"* **No per-bet transaction.** A ten-second round cannot wait for a block, and nobody will approve forty transactions an hour. One deposit in, one settlement out; the rounds in between are the device's. * **No anti-tamper on the device.** Prices and odds are computed on the handheld. That is fine for a device you hand someone; it is not fine for a product with an adversary, and the honest place to move it is a signed price attestation the escrow checks. * **No account system.** The wallet is the account, the name is the profile, and the leaderboard is a read. There is nothing to sign up for and"},{"u":"writing-a-game.html","p":"Writing a game","t":"Writing a game","a":"writing-a-game","x":"From nothing to a playable game in about twenty minutes. Read [PLAN.md](../PLAN.md) first if you want the reasoning; this page is the doing. ---"},{"u":"writing-a-game.html","p":"Writing a game","t":"1. Scaffold","a":"1-scaffold","x":"tick new dropzone python dropzone.py You now have a playable game on simulated prices: crank the dial, press A, and a box settles at the next bell. Open `dropzone.py` \u2014 it is about eighty lines, and every one of them is yours to change. ---"},{"u":"writing-a-game.html","p":"Writing a game","t":"2. The five hooks","a":"2-the-five-hooks","x":"A game is a class. Override what you need; the rest has a default. from tick import Game, run class Dropzone(Game): title = 'DROPZONE' window_s = 10.0 def setup(self, ctx): # once, everything is already running self.offset = 0 def on_action(self, ctx, action): # UP / DOWN / A / B ... def on_settle(self, ctx, settlement): # your bet was paid or lost ... def update(self, ctx, dt): # every frame, before drawing ... def draw(self, ctx, screen): # every frame ... run(Dropzone()) The full list: `setup`, `teardown`, `on_tick`, `on_round_start`, `on_round_end`, `on_settle`, `on_action`, `on_touch`, `u"},{"u":"writing-a-game.html","p":"Writing a game","t":"3. Placing a bet","a":"3-placing-a-bet","x":"One call. It debits, prices, and books the bet for the coming bell. def on_action(self, ctx, action): if action.name == 'A': bet = ctx.bet(low, high) if bet is None: ctx.note(ctx.refused) # put the reason on screen ctx.play('warn') else: ctx.play('buy1') `ctx.bet` returns `None` and sets `ctx.refused` when: | `ctx.refused` | Meaning | |---|---| | `NO MARKET YET` | no price has arrived; the clock has not started | | `READING THE MARKET` | the feed has not moved enough to measure anything | | `FEED STALE` | the last price is too old to settle money on | | `MARKET QUIET / NO BETS` | the price has"},{"u":"writing-a-game.html","p":"Writing a game","t":"What a bet is worth, before placing it","a":"what-a-bet-is-worth-before-placing-it","x":"ctx.quote(low, high) # multiple, at this instant ctx.quote(low, high, rounds_ahead=2) # for the bell after next: a longer bet Draw the quote next to the cursor and it updates as the market moves \u2014 which is most of what makes these games readable. ---"},{"u":"writing-a-game.html","p":"Writing a game","t":"4. Shaping the bet","a":"4-shaping-the-bet","x":"Almost every game is one of three shapes. **A box** \u2014 a range around a level the player aims at: half = ctx.open_price * 4 / 10_000 # 4 bps: a fixed height centre = ctx.open_price + self.offset * step ctx.bet(centre - half, centre + half) **A side** \u2014 everything above or below a line: ctx.bet(ctx.open_price, 1e9) # up ctx.bet(0.0, ctx.open_price) # down **A ladder** \u2014 several ranges at once, each its own bet, all settling together: for low, high in self.rungs(ctx): ctx.bet(low, high, stake=ctx.stake // len(self.rungs(ctx)))"},{"u":"writing-a-game.html","p":"Writing a game","t":"Size geometry in basis points, not in volatility","a":"size-geometry-in-basis-points-not-in-volatility","x":"def unit(self, ctx): return ctx.open_price / 10_000 # one basis point of the round's open Anchor on the **round's opening price** and size in **basis points**, so the box is the same size all round and in every market. Sizing it in sigma means the market redraws a box the player already bought \u2014 confusing, and dishonest about what they paid for. If you change `window_s`, scale the geometry rather than leaving it: from tick.pricing import scale_to_window box_bps = scale_to_window(8.0, self.window_s, reference_s=20.0) Prices travel with the square root of time. Halving the round without shrinkin"},{"u":"writing-a-game.html","p":"Writing a game","t":"5. Locking, topping up, and arming","a":"5-locking-topping-up-and-arming","x":"Three small rules that make these games feel fair. class MyGame(Game): arm_s = 1.5 # swallow presses for a beat after each bell def on_action(self, ctx, action): if action.name in ('UP', 'DOWN'): if ctx.next_bets: # already bought: lock the dial ctx.note('LOCKED / A ADDS ' + ctx.usdc()) ctx.play('lock') return self.offset += 1 if action.name == 'UP' else -1 elif action.name == 'A': bets = ctx.next_bets low, high = (bets[0].low, bets[0].high) if bets else self.aim(ctx) ctx.bet(low, high) # tops up at *this* press's odds * **The first press fixes the level.** Repositioning after the market moves"},{"u":"writing-a-game.html","p":"Writing a game","t":"6. Drawing","a":"6-drawing","x":"The SDK gives you a ladder, a HUD and a palette so a one-file game still looks like it belongs on the device. from tick.hud import (Ladder, clear, draw_bet, draw_clock, draw_cursor, draw_hud, draw_open_line, draw_price_line) from tick.ui import CREAM, MINT, MUTED, RED, YELLOW, say, say_right def draw(self, ctx, screen): clear(screen) ladder = Ladder(ctx.open_price, 20 * self.unit(ctx)) # price -> pixels draw_open_line(screen, ladder, ctx.open_price) # the reference draw_price_line(screen, ladder, ctx.market.history, ctx.now, right_x=170) for bet in ctx.live_bets: # settling this bell draw_bet("},{"u":"writing-a-game.html","p":"Writing a game","t":"7. Sound","a":"7-sound","x":"You get the generic cues for free \u2014 the bell, the swoops when the price crosses into or out of a live bet, the countdown pitched by whether you are currently winning, the win/miss/void stings, and a music bed chosen from what is at stake. Turn them off with `sound_cues = False` / `music = False`. Add your own from the same kit: ctx.play('buy2') ctx.play(ctx.sounds.detent(abs(self.offset) / self.reach)) # rising crank clicks The two that matter most are the crossing swoops and the pitched countdown: together they tell a player whether they are winning **without looking**, which is the whole poi"},{"u":"writing-a-game.html","p":"Writing a game","t":"8. Testing it","a":"8-testing-it","x":"A game that holds money needs tests, and a game loop is testable only if it can run without a window. # tests/test_dropzone.py import unittest from tick import InputAction from tick.testing import Harness from dropzone import Dropzone class TestDropzone(unittest.TestCase): def test_a_bet_is_taken_and_settled(self): with Harness(Dropzone(), seed=7) as h: h.warm(25) # 25s of market, instantly h.press(InputAction.A) self.assertEqual(h.balance, 1000_000000 - h.ctx.stake) h.advance(25) self.assertTrue(h.book.history) def test_a_certainty_is_never_sold(self): with Harness(Dropzone(), seed=7) as h: h"},{"u":"writing-a-game.html","p":"Writing a game","t":"9. Going live","a":"9-going-live","x":"tick run dropzone.py --source coinbase # real prices, paper money tick run dropzone.py --source substreams # on-chain prices TICK_FUNDING=arc-testnet TICK_STAKE_USDC=0.001 python dropzone.py # real money Nothing in your game changes for any of these. See [prices-substreams.md](prices-substreams.md), [money-arc.md](money-arc.md) and [identity-ens.md](identity-ens.md). ---"},{"u":"writing-a-game.html","p":"Writing a game","t":"Checklist before you call it done","a":"checklist-before-you-call-it-done","x":"- [ ] Every refusal reason reaches the screen, in words. - [ ] The dial does nothing surprising after a bet is placed \u2014 and says so. - [ ] The view is anchored on the round's open price. - [ ] Geometry is in basis points; only the payout reacts to the market. - [ ] `arm_s` is set if a press can be meant for the round that just closed. - [ ] Your game draws correctly with no price yet, and with a stale feed. - [ ] A test asserts what happens when the market goes flat. - [ ] A test asserts a settlement's payout, not just that one happened."},{"u":"prices-substreams.html","p":"Prices on-chain","t":"A price your game can settle on","a":"a-price-your-game-can-settle-on","x":"Every bet in this SDK settles on one number at one moment. That number has to be **live**, **hard to push**, and **honest about its own age** \u2014 otherwise the fairest odds in the world are being applied to the wrong price. This page is how the SDK gets such a number out of live chain data, and how you point it at a different market, a different chain, or a different set of pools. ---"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Why an exchange ticker is not enough","a":"why-an-exchange-ticker-is-not-enough","x":"`TICK_MARKET_SOURCE=coinbase` is fine while you build. It is one venue, behind a REST cache, with an unknown number of hops between the trade and you. For a game that pays out on the price at a bell, three problems matter: 1. **It goes quiet.** A quiet ticker looks exactly like a dead one. 2. **It is one book.** One venue's price at one instant is one venue's opinion. 3. **You cannot check it afterwards.** A player who thinks they were robbed has nothing to look at. Reading swaps directly off-chain fixes all three: the data is public, it is timestamped by the block, and a settled round can be "},{"u":"prices-substreams.html","p":"Prices on-chain","t":"The shape of it","a":"the-shape-of-it","x":"chain blocks \u2502 \u25bc Substreams module (tick_eth_price) runs on The Graph's network \u2502 per block, per pool: price, in-range liquidity, tx hash \u25bc substreams run --output jsonl \u00d7N chains \u2502 JSON lines on stdout \u25bc relay.py (your laptop or a small server) \u2502 GET /stream \u2192 newline-delimited JSON over one long-lived connection \u25bc tick.feeds.SubstreamsFeed \u2192 PriceTick \u2192 your game tick.feeds.PoolStream \u2192 Block / PoolPrice (raw, per pool) The relay exists for one reason: **the API key must not live on a handheld somebody can pick up**. The device holds a plain HTTP connection and receives blocks as they are pr"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"The module","a":"the-module","x":"The Substreams module reads Uniswap v3 and v4 swaps and emits, for every block, each watched pool's price and in-range liquidity. Three properties of it are what make the SDK's rules work: * **It emits on every block, traded or not.** That is what lets `Market` tell a *calm* market from a *dead* one \u2014 the distinction the whole \"quiet market sells nothing\" rule rests on. A feed that only speaks when something happens cannot make that distinction, and a game built on one will eventually sell a certainty at 2x. * **Every message carries its own block time.** `SubstreamsFeed` turns that into `Pric"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Where the pool list comes from","a":"where-the-pool-list-comes-from","x":"A pool's `sqrtPriceX96` is meaningless without its **token order and decimals**. Get either wrong and the price is off by a factor of 10\u00b9\u00b2 and the game happily settles on it. Standardized DEX subgraphs hold both, for every pool, in one schema that is the same across protocols and chains. That is what makes pool discovery a single query sent unchanged to every subgraph you list: { protocols(first: 1) { name network } liquidityPools(first: 20, where: { inputTokens: [\"<WETH>\", \"<USDC>\"] }) { id name inputTokens { id symbol decimals } inputTokenBalances fees { feeType feePercentage } } } python3 p"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Many pools, one number","a":"many-pools-one-number","x":"`PoolBook` combines them. The rule is **average weighted by in-range liquidity, after discarding any pool more than 50 bps from the weighted median.** Each half of that does a job: | Approach | What it gets right | What it gets wrong | |---|---|---| | One pool | simple | one large trade at the bell moves your settlement price | | Median of pools | very hard to push | pins to a single pool, usually the biggest, which can go a minute without a trade \u2014 the price stops moving | | Plain average | moves on any trade anywhere | one broken or pushed pool drags the whole number | | **Trim, then weight*"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Running it","a":"running-it","x":"# 1. get a Substreams key and build the module (see substreams/) substreams auth cargo build --target wasm32-unknown-unknown --release # 2. start the relay on a machine that can keep a connection open python3 substreams/relay.py --port 8787 # relay on http://192.168.1.24:8787/stream # 3. point the device at it echo 'TICK_MARKET_SOURCE=substreams' >> .env echo 'SUBSTREAMS_BASE_URL=http://192.168.1.24:8787' >> .env # 4. check it before you trust it tick pools -v `tick pools -v` prints every block, every pool, and how far each pool is from the combined price in basis points. If that is quiet or n"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Using it in a game","a":"using-it-in-a-game","x":"Nothing changes in your game. That is the point of the seam: class MyGame(Game): def on_action(self, ctx, action): if action.name == 'A': ctx.bet(ctx.price - 1, ctx.price + 1) # same code on every feed tick run mygame.py --source sim # develop tick run mygame.py --source substreams # demo"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Reading the pools yourself","a":"reading-the-pools-yourself","x":"If your game wants more than one number \u2014 a spread between chains, a per-venue display, a \"which pool moved\" callout \u2014 take the raw stream: from tick import PoolStream for block in PoolStream('http://localhost:8787').blocks(): print(block.chain, block.number, block.age(time.time())) for pool in block.prices: print(' ', pool.pool[:10], pool.price, pool.liquidity) `PoolStream` reconnects on its own and yields `Block` objects with validated per-pool prices. See [`examples/pools.py`](../examples/pools.py). ---"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Checking a settled round afterwards","a":"checking-a-settled-round-afterwards","x":"Each price the module emits carries the hash of the transaction behind it. That means a round settled at 14:32:07 can be traced to the exact swap that set the price \u2014 by the player, after the fact, with no trust in the device at all. If you are going to take someone's money on a number, being able to show them the trade is worth more than any amount of UI. ---"},{"u":"prices-substreams.html","p":"Prices on-chain","t":"Recording it","a":"recording-it","x":"Live markets are a terrible thing to develop against: you cannot make the price do what you need to see, and you cannot make it do it twice. Record once, replay forever: tick record -o eth.ndjson --source substreams --seconds 600 tick run mygame.py --source replay # TICK_REPLAY_FILE=eth.ndjson Replay reproduces the recorded `source_age` too, so the gap that voided a round in the wild voids it again on your desk."},{"u":"money-arc.html","p":"Real money","t":"Real money, without asking anyone to trust the box","a":"real-money-without-asking-anyone-to-trust-the-box","x":"A game with paper money is a demo. The moment it holds real funds, four questions appear, and they have to be answered in the design rather than in a disclaimer: 1. How does money get **in**, from a wallet the device has never met? 2. How does a ten-second round settle **without a transaction per bet**? 3. How does a player get their money **out**, without trusting the device? 4. What stops the screen promising a payout that cannot be funded? This page is how the SDK answers them, and how you point it at your own chain or your own contract. ---"},{"u":"money-arc.html","p":"Real money","t":"The session model","a":"the-session-model","x":"player's wallet \u2500\u2500(1) plain USDC send\u2500\u2500\u25b6 device address \u2502 (2) openFor(player, amount, device) \u25bc TickEscrow \u2500\u2500 holds the deposit, in the player's name, \u2502 plus reserved house money for the win (3) rounds run on the device, off-chain, for as long as they play \u2502 (4) close(sessionId, finalBalance) \u25bc payout \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25b6 player's wallet **One deposit in, one settlement out.** Everything between is the device's, and that is deliberate: a ten-second round cannot wait for a block, and nobody will approve forty transactions an hour. Sessions are what make an arcade cadence possible on-chain at all. ---"},{"u":"money-arc.html","p":"Real money","t":"1. Money in: a send, not a connect","a":"1-money-in-a-send-not-a-connect","x":"The device shows **its own address as a QR code** (EIP-681, so scanning it opens a send screen prefilled with the right chain). The player sends USDC from their own wallet. No dApp, no WalletConnect, no signature prompt on a 480\u00d7320 screen, no app to install. funding.qr_text # 'ethereum:0x11ab...@5042002' The device watches the chain for transfers to itself, keeps a small fixed gas fee, and locks the rest in escrow **in the sender's name**: sid, deposit, reserve, tx = chain.open_for(player=sender, amount=amount) Two details that make this work in practice: * **USDC is the gas token**, so the d"},{"u":"money-arc.html","p":"Real money","t":"2. Rounds run off-chain","a":"2-rounds-run-off-chain","x":"Bets are placed, topped up and settled entirely on the device against `wallet.balance`. The chain sees none of it, which is why a round can be ten seconds long. This is the honest trade: the device is trusted to run the rounds for the length of a session, and the contract is trusted with the money. If you need the rounds trusted too, the seam to use is a signed price attestation the escrow checks at close \u2014 the settlement path already takes a signature (see below), so that is an extension rather than a redesign."},{"u":"money-arc.html","p":"Real money","t":"3. Money out: the contract pays, not the device","a":"3-money-out-the-contract-pays-not-the-device","x":"funding.cash_out(wallet) # the worker closes each session at the final balance `close(sessionId, finalBalance)` pays **the address the USDC came from**. The device cannot redirect it, cannot skim it, and cannot keep it: the payout target was fixed when the session opened. Two safety valves in the contract matter to your game's UX: * The device can either send `close` itself, or sign an EIP-712 `Close(sessionId, finalBalance)` that anyone submits \u2014 so a device with no gas left can still settle. * If a device never closes at all, the player can `reclaim` their deposit after a timeout fixed when "},{"u":"money-arc.html","p":"Real money","t":"4. The cap: never promise what cannot be paid","a":"4-the-cap-never-promise-what-cannot-be-paid","x":"Opening a session reserves house money \u2014 `deposit \u00d7 winCapBps`, 4x by default \u2014 so a win is always payable. That ceiling is exposed all the way up to your game: wallet.cap # deposit + reserve, or None with no session open ctx.would_exceed_cap(...) # would one more bet's best case go past it? and `ctx.bet` refuses with `MAX WIN REACHED / CASH OUT` before taking the stake. Without this check the screen quotes a 20x payout the contract will cap, and the player finds out at settlement \u2014 which is the single worst moment to discover it. ---"},{"u":"money-arc.html","p":"Real money","t":"Using it","a":"using-it","x":"pip install -e \".[chain]\" cat >> .env <<'EOF' TICK_FUNDING=arc-testnet TICK_STAKE_USDC=0.001 # real money: make the stakes small while you test EOF python mygame.py Your game code does not change. `TICK_FUNDING` swaps the wallet's funding backend; `ctx.wallet` behaves identically either way. # the only place a game usually cares: if ctx.wallet.onchain: draw_qr(screen, ctx.wallet.funding.qr_text)"},{"u":"money-arc.html","p":"Real money","t":"State, restarts, and threads","a":"state-restarts-and-threads","x":"* Every chain call runs on **one worker thread**. The game thread only drains a queue in `sync()`. A slow RPC can never cost a frame, and a dropped frame on a handheld is felt immediately. * Open sessions, the balance and the last block read are saved atomically under `.tick/`. A restart resumes play and **finishes an interrupted cash-out** rather than stranding a session. * The device key is created on first run in `.tick/device.json`, mode 0600, and never leaves. It is not the player's key and holds nothing but gas."},{"u":"money-arc.html","p":"Real money","t":"Pointing it somewhere else","a":"pointing-it-somewhere-else","x":"# tick/chain.py NETWORKS['my-chain'] = Network( name='MY CHAIN', chain_id=1234, rpc='https://rpc.example', explorer='https://explorer.example', usdc='0x...', escrow='0x...') or, without touching code: TICK_ARC_RPC=https://my-rpc TICK_ESCROW_ADDRESS=0xMyEscrow python mygame.py The escrow interface the SDK needs is four calls \u2014 `openFor`, `close`, `sessions`, and the house view (`freeHouse`, `winCapBps`, `maxSessionDeposit`, `paused`) \u2014 so a different contract with the same shape drops in. ---"},{"u":"money-arc.html","p":"Real money","t":"Testing it without a chain","a":"testing-it-without-a-chain","x":"Every on-chain call sits behind `Chain`, so the whole money state machine is testable offline with a fake: class FakeChain: def block(self): return self.head def incoming(self, first, last): ... def house(self): return House(free, cap_bps, most, paused) def open_for(self, player, amount): ... def close(self, sid, final): ... funding = EscrowFunding('arc-testnet', chain=FakeChain(), start=False) funding.step() [`tests/test_escrow.py`](../tests/test_escrow.py) covers, in milliseconds and with no network: a deposit opening a session in the sender's name, the same transfer never being taken twice,"},{"u":"identity-ens.html","p":"Player names","t":"Giving your players a name","a":"giving-your-players-a-name","x":"A leaderboard of `0x7ee8\u2026CCac` is a leaderboard nobody reads. A player cannot recognise themselves on it, cannot point at it, and cannot show it to anyone. The usual fix is an account system: a sign-up, a database, a server. For a handheld you hand to a stranger at a table, that is three things too many \u2014 and all three die the day you stop paying for them. The SDK does it with ENS instead. Every wallet that plays gets a readable subname, and that player's record lives in the name's own text records. The leaderboard is then **derived, not stored**: read the registry, read each name, sort. ---"},{"u":"identity-ens.html","p":"Player names","t":"What a player gets","a":"what-a-player-gets","x":"0x7ee85B080701330bf53Be62B7E72fcDD034eCCac \u25bc amber-otter.tick.eth \u251c\u2500\u2500 addr 0x7ee8\u2026CCac (points back at the wallet) \u251c\u2500\u2500 tick.sessions 7 \u251c\u2500\u2500 tick.wins 4 \u251c\u2500\u2500 tick.pnl 12.4 \u251c\u2500\u2500 tick.best 8.1 \u251c\u2500\u2500 tick.last_tx 0x97c9\u2026bfaf \u251c\u2500\u2500 avatar (theirs to set) \u2514\u2500\u2500 description (theirs to set) The name is a real ENS name. It resolves in any ENS client, it outlives the game, and the record belongs to the name rather than to your backend. ---"},{"u":"identity-ens.html","p":"Player names","t":"The handle is derived from the address","a":"the-handle-is-derived-from-the-address","x":"This is the part worth stealing. from tick.identity import handle, name_of handle('0x7ee85B08...') # 'amber-otter' name_of('0x7ee85B08...') # 'amber-otter.tick.eth', or None `handle()` hashes the wallet and indexes two word lists. It is deterministic, so **the device can work out which name to look for without an index, an API, or a lookup service** \u2014 it computes the candidate and asks the resolver whether that name points back at this wallet: for attempt in range(4): name = f'{handle(address, attempt)}.{parent}' if addr_of(resolver, name).lower() == address.lower(): return name # trusted: the"},{"u":"identity-ens.html","p":"Player names","t":"Who may write what","a":"who-may-write-what","x":"Two sets of records, split by who can write them: | Records | Written by | Why | |---|---|---| | `tick.sessions`, `tick.wins`, `tick.pnl`, `tick.best`, `tick.last_tx` | the scorekeeper only | a player who can write their own score has no score | | `avatar`, `description`, `url`, `com.twitter` | the player | it is their name | Enforced by resolver roles, not by convention. The scorekeeper holds the key with permission to write the `tick.*` keys; a player holds their name and can write the profile keys. **Everything in `tick.identity` is read-only.** Writing stats is a privileged, single-instanc"},{"u":"identity-ens.html","p":"Player names","t":"Reading it back","a":"reading-it-back","x":"One name's records, in **one call**: from tick.chain import Rpc from tick.identity import ens, records_of rpc = Rpc(ens().rpc) records_of(rpc, 'amber-otter.tick.eth') # {'tick.sessions': '7', 'tick.wins': '4', 'tick.pnl': '12.4', ...} That goes through ENS's Universal Resolver with a `multicall`, so seven records cost one round trip rather than seven. On a handheld drawing a leaderboard of a dozen players, that is the difference between a board and a spinner. The whole board: from tick.identity import Standings for row in Standings().read(): print(row.name, row.pnl, row.wins, row.sessions) `St"},{"u":"identity-ens.html","p":"Player names","t":"On a device, without stalling a frame","a":"on-a-device-without-stalling-a-frame","x":"from tick.identity import Leaderboard class MyGame(Game): def setup(self, ctx): self.board = Leaderboard() def on_action(self, ctx, action): if action.name == 'B': self.board.showing = True self.board.want() # starts the reader thread on first use def draw(self, ctx, screen): for row in self.board.rows or []: ... if self.board.rows is None: say(screen, self.board.error or 'READING ENS...', 240, 150, 16, MUTED) Nothing is read until it is first wanted; it refreshes while the board is on screen and sleeps while it is not; and when the RPC is unreachable it **keeps the last rows and says so** rat"},{"u":"identity-ens.html","p":"Player names","t":"Pointing it at your own name","a":"pointing-it-at-your-own-name","x":"Nothing in `tick.identity` is hardcoded to one parent: TICK_ENS_PARENT=mygame.eth TICK_ENS_RPC=https://ethereum-sepolia-rpc.publicnode.com TICK_ENS_REGISTRY=0x... # your parent's subname registry TICK_ENS_RESOLVER=0x... # the one resolver every subname uses TICK_ENS_SINCE=11684278 # first block: where a leaderboard starts reading or in code: from tick.identity import Ens, Standings mine = Ens('mygame.eth', rpc, chain_id, registry, resolver, since) Standings(net=mine).read() You can change the stat keys too \u2014 `Standings(keys=('mygame.score', ...))` \u2014 since `records_of` takes whatever list you g"},{"u":"identity-ens.html","p":"Player names","t":"Why names live on their own chain","a":"why-names-live-on-their-own-chain","x":"Names and records sit on a cheap public chain; the money settles wherever your escrow is. They are deliberately independent: * Identity is **public and permanent** and wants to be read by things that are not your game. Settlement is **fast and private to a session** and wants to be cheap. * A leaderboard being briefly unreachable must never stop a player cashing out, and a settlement chain being busy must never stop a name resolving. ENSIP-11 gives you the crossing point when you want one: an EVM chain's coin type is `0x80000000 | chain_id`, so a name can carry an address record for the chain "},{"u":"identity-ens.html","p":"Player names","t":"The one rule","a":"the-one-rule","x":"**Never let the device write a score.** Read names on the device, write them from one service that holds the key. The device is a thing you hand to strangers; treat anything it can sign as public."},{"u":"api.html","p":"API","t":"The public surface, on one page","a":"the-public-surface-on-one-page","x":"Everything you can import, what it does, and what it returns. Imports below are what actually works; `from tick import X` covers the common ones. ---"},{"u":"api.html","p":"API","t":"tick \u2014 the top level","a":"tick-the-top-level","x":"from tick import Game, GameContext, run, Runtime, InputAction from tick import Market, open_feed, PriceTick, Asset, ASSETS, PoolStream from tick import Wallet, MICRO, parse_usdc, format_usdc, stake_micro from tick import RoundClock, Round, Bet, BetBook, Settlement from tick import probability, multiple, sigma, scale_to_window ---"},{"u":"api.html","p":"API","t":"Game \u2014 what you subclass","a":"game-what-you-subclass","x":"| Attribute | Default | Meaning | |---|---|---| | `title` | `'TICK GAME'` | window title and HUD label | | `window_s` | `10.0` | round length; `TICK_WINDOW_S` overrides | | `stake_usdc` | `None` | per bet; `None` means `TICK_STAKE_USDC` | | `max_multiple` | `25.0` | never quote more than this | | `min_multiple` | `1.05` | never sell under this | | `house_edge` | `0.0` | `0.03` keeps 3% | | `rounds_ahead` | `1` | `1` = the next bell; `0` = the round already running | | `arm_s` | `0.0` | swallow bets for this long after a bell | | `sound_cues` | `True` | the SDK plays the generic cues | | `music"},{"u":"api.html","p":"API","t":"GameContext \u2014 what every hook is handed","a":"gamecontext-what-every-hook-is-handed","x":"**State** | | | |---|---| | `ctx.price` | last price | | `ctx.open_price` | the round's opening price \u2014 anchor your view here | | `ctx.move` | how far the price has come since the round opened | | `ctx.remaining` | seconds to the bell | | `ctx.round` | the `Round` now running | | `ctx.now` / `ctx.dt` / `ctx.frame` | monotonic seconds, frame delta, frame count | | `ctx.armed` | `False` for `arm_s` after a bell | | `ctx.stake` | micro-USDC per bet | **Betting** | | | |---|---| | `ctx.bet(low, high, stake=None, rounds_ahead=None, label='')` | place; `None` if refused | | `ctx.quote(low, high, rou"},{"u":"api.html","p":"API","t":"tick.feeds","a":"tickfeeds","x":"open_feed(source=None, seed=None, asset=None, **kwargs) -> BaseFeed `source`: `sim` \u00b7 `coinbase` \u00b7 `substreams` \u00b7 `replay` \u00b7 `frozen`; defaults to `TICK_MARKET_SOURCE`. | | | |---|---| | `feed.poll(dt, now) -> list[PriceTick]` | non-blocking | | `feed.stream(interval=0.05, limit=None)` | blocking generator, for scripts | | `feed.close()` \u00b7 `feed.name` \u00b7 `feed.status` \u00b7 `feed.asset` | | | Class | Notes | |---|---| | `SimulatedFeed(seed, asset, volatility=None)` | 20 Hz seeded walk | | `FrozenFeed(price, asset)` | never moves; for testing refusals | | `CoinbaseFeed(asset)` | 5 Hz public REST, wo"},{"u":"api.html","p":"API","t":"tick.market","a":"tickmarket","x":"Market(feed=None, source=None, seed=None, **kwargs) | | | |---|---| | `.poll(dt, now=None)` | read the feed and fold in; returns accepted ticks | | `.accept(tick, now) -> bool` | validate and fold one tick | | `.price` `.tick` `.history` `.asset` `.name` `.status` | | | `.fresh(now)` `.quiet(now)` `.ready` | | | `.sellable(now)` `.why_not(now)` | may a bet be sold, and why not | | `.variance` | realized variance per second (cached) | | `.sigma(horizon)` | one standard deviation over that horizon | | `.probability(low, high, horizon)` | chance of landing in the range | | `.quote(low, high, hori"},{"u":"api.html","p":"API","t":"tick.pricing","a":"tickpricing","x":"normal_cdf(x) sigma(price, variance_per_second, horizon) probability(price, low, high, variance_per_second, horizon) multiple(chance, edge=0.0, cap=25.0) scale_to_window(value_bps, window_s, reference_s=20.0) annualized(variance_per_second) ---"},{"u":"api.html","p":"API","t":"tick.money","a":"tickmoney","x":"Wallet(balance=100*MICRO, funding=None) `.balance` `.debit(n) -> bool` `.credit(n)` `.can_afford(n)` `.cap` `.live` `.onchain` `.load(usdc)` `.would_exceed_cap(best)` MICRO # 1_000_000 to_micro(1.5) # 1_500_000 (truncates) parse_usdc('0.57') # 570000 (exact; raises on >6 decimals) format_usdc(570000, 2) # '0.57' places_for(1000) # 3 stake_micro() # from TICK_STAKE_USDC DemoFunding() # paper money ---"},{"u":"api.html","p":"API","t":"tick.rounds","a":"tickrounds","x":"RoundClock(window_s=10.0, void_after=None) `.start(price, now)` \u00b7 `.update(now, price, settleable) -> list[Round]` `.remaining(now)` `.progress(now)` `.horizon(now, rounds_ahead)` `.move(price)` `.index` `.open_price` `.ends_at` `.settling` `.current` `.closed` `Round(index, open_price, opened_at, ends_at)` \u2192 `.close_price` `.voided` `.move` `.remaining(now)` `.progress(now)` ---"},{"u":"api.html","p":"API","t":"tick.bets","a":"tickbets","x":"BetBook(wallet) `.place(round_index, low, high, stake, multiple, label='') -> Bet | None` `.settle(round_) -> list[Settlement]` \u00b7 `.refund(round_index)` \u00b7 `.refund_all()` \u00b7 `.for_round(i)` \u00b7 `.staked()` \u00b7 `.best_case()` \u00b7 `.last` \u00b7 `.streak` \u00b7 `.history` \u00b7 `.open` `Bet` \u2192 `.low` `.high` `.level` `.half` `.stake` `.payout` `.multiple` `.presses` `.label` `.contains(price)` `Settlement` \u2192 `.hit` `.price` `.stake` `.payout` `.multiple` `.voided` `.net` ---"},{"u":"api.html","p":"API","t":"tick.escrow and tick.chain","a":"tickescrow-and-tickchain","x":"build_wallet(kind=None) # TICK_FUNDING: 'demo' or 'arc-testnet' EscrowFunding(kind, chain=None, state_dir='.tick', gas_fee=None, start=True) `.qr_text` `.in_session` `.cap` `.player` `.status` `.busy` `.error` `.sync(wallet)` `.cash_out(wallet)` `.step()` `.stop()` `.tx_url(tx)` Chain(network(kind), load_device()) # block, incoming, house, open_for, close, transfer Rpc(url)(method, *params) NETWORKS['arc-testnet'] ---"},{"u":"api.html","p":"API","t":"tick.identity","a":"tickidentity","x":"handle(address, attempt=0) # 'amber-otter' name_of(address, net=None, rpc=None) # 'amber-otter.tick.eth' | None records_of(rpc, name, keys=STATS) # one Universal Resolver call Standings(net=None, rpc=None, keys=STATS).read() -> list[Standing] Leaderboard(read=None) # threaded: .want() .rows .error .showing namehash(name) \u00b7 dns_encode(name) \u00b7 coin_type(chain_id) \u00b7 ens() ---"},{"u":"api.html","p":"API","t":"tick.hud and tick.ui","a":"tickhud-and-tickui","x":"clear(s) \u00b7 draw_hud(ctx, s, back, go) \u00b7 draw_clock(ctx, s, x, y) Ladder(centre, half, top=34, bottom=268) # .y_of .price_at .rect .clamped_y draw_grid \u00b7 draw_open_line \u00b7 draw_bet \u00b7 draw_cursor \u00b7 draw_price_line NAVY PANEL GRID CREAM MUTED YELLOW MINT RED font(size) \u00b7 label(...) \u00b7 say(...) \u00b7 say_right(...) \u00b7 footer(...) \u00b7 dashed_rect(...) Sounds() # .play(cue) .music(mode) .detent(fraction) .ok **Cues:** `bell` `hot` `cold` `tick_in` `tick_out` `buy1` `buy2` `buy3` `win_small` `win_big` `jackpot` `hattrick` `fizzle` `void` `warn` `stale` `crossline` `nav` `enter` `back` `coin` `move` `select` `"},{"u":"api.html","p":"API","t":"tick.input and tick.display","a":"tickinput-and-tickdisplay","x":"InputAction.UP / DOWN / A / B / QUIT actions_from_event(event) \u00b7 event_position(event) \u00b7 HeldKeys() EncoderInput.try_open(clk=21, dt=20, sw=16, invert=False) # None off-Pi ButtonPad.try_open(red=13, yellow=26) # None off-Pi WIDTH, HEIGHT = 480, 320 ; FPS = 30 bootstrap() \u00b7 init_display(title) \u00b7 present() \u00b7 to_canvas(fx, fy) display_to_canvas(pos) \u00b7 rotation() ---"},{"u":"api.html","p":"API","t":"tick.testing","a":"ticktesting","x":"Harness(game, feed=None, source='sim', seed=7, balance=1000*MICRO, stake=None, window_s=None, start=1000.0) `.warm(25)` `.advance(seconds)` `.frame(dt, actions, touches)` `.press(action)` `.touch(pos)` `.draw_once()` `.close()` \u00b7 `.ctx` `.market` `.wallet` `.rounds` `.book` `.price` `.balance` `.settled` FakeClock(start=1000.0) # a monotonic clock you move by hand ScriptedFeed([(t, price), ...]) # exactly the ticks you list ---"},{"u":"api.html","p":"API","t":"tick.env","a":"tickenv","x":"load_env_file(path=None) \u00b7 load_env_near(start) \u00b7 flag(name, default) number(name, default) \u00b7 text(name, default)"},{"u":"device.html","p":"The device","t":"The device","a":"the-device","x":"480\u00d7320 landscape, a rotary dial, two buttons, a speaker. Everything here is handled for you; this page is what to do when it is not. ---"},{"u":"device.html","p":"The device","t":"The canvas","a":"the-canvas","x":"Your game draws on a **480\u00d7320 landscape surface** and never thinks about the display again. If the panel underneath is portrait \u2014 a 320\u00d7480 SPI screen, say \u2014 each finished frame is turned onto it, and touch positions are turned back the other way so a tap lands where the player pointed. TICK_ROTATE=auto # turn only when the screen is portrait, and only on Linux TICK_ROTATE=90 # force it That seam earns its keep: a game written against a rotating canvas has rotation maths smeared through its draw code *and* its input code, and it is wrong in a different way on every device. from tick.display i"},{"u":"device.html","p":"The device","t":"Controls","a":"controls","x":"Four actions. A dial, two buttons, a keyboard and a touchscreen all collapse into them \u2014 which is the only reason a game is testable at all. | Action | Dial | Panel | Keyboard | Touch | |---|---|---|---|---| | `UP` / `DOWN` | turn | \u2014 | \u2191 \u2193 / W S (hold to spin) | tap above / below | | `A` | short click | yellow | Enter \u00b7 Space \u00b7 \u2192 \u00b7 X | \u2014 | | `B` | long click (0.65 s) | red | Backspace \u00b7 \u2190 \u00b7 Z | \u2014 | | `QUIT` | \u2014 | \u2014 | Escape | window close |"},{"u":"device.html","p":"The device","t":"Wiring (BCM numbering, not header position)","a":"wiring-bcm-numbering-not-header-position","x":"encoder CLK GPIO21 (pin 40) DT GPIO20 (pin 38) SW GPIO16 (pin 36) VCC 3.3 V GND GND red GPIO13 (pin 33) \u2500\u2500 button \u2500\u2500 GND (pin 34 or 39) back / B yellow GPIO26 (pin 37) \u2500\u2500 button \u2500\u2500 GND (pin 34 or 39) go / A Each button shorts its pin to ground against the internal pull-up; nothing else is needed in hardware. Presses are edge-triggered, so holding repeats nothing, and a button already held when the game starts is ignored rather than firing on the first frame. Both readers are `try_open()`: on a machine with no GPIO they return `None` and the game plays on the keyboard rather than refusing to st"},{"u":"device.html","p":"The device","t":"Why the dial is read two ways","a":"why-the-dial-is-read-two-ways","x":"encoder.poll(continuous=True) # in play: this poll's motion only encoder.poll(continuous=False) # in menus: rate-limited In play, every detent counts and a stopped crank must stop moving the box at once \u2014 so queued motion is discarded. In a menu, a whip-spin would otherwise wrap the list a dozen times, so it is rate-limited to one step per 50 ms. ---"},{"u":"device.html","p":"The device","t":"Sound","a":"sound","x":"Everything is synthesized at startup from square waves \u2014 no audio assets, no dependencies, about 0.16 s to build. See the cue list in [api.md](api.md#tickhud-and-tickui). Sound is optional **by construction**: if the mixer cannot open, every `play` and `music` becomes a no-op and the game runs silently. It never raises. TICK_SOUND=0 # off"},{"u":"device.html","p":"The device","t":"On a Raspberry Pi 5","a":"on-a-raspberry-pi-5","x":"There is no analog jack, so ALSA alone offers only the two HDMI outputs, and opening one fails with nothing connected. Sound works through the **pipewire-pulse** server instead. Do not force `SDL_AUDIODRIVER=alsa`: it bypasses the sound server and finds only the dead HDMI sinks. Bluetooth adds 100\u2013200 ms of latency, so crank clicks trail the dial. A USB audio dongle is tighter. An I\u00b2S DAC is tightest, but it wants GPIO18/19/21 \u2014 and GPIO21 is the encoder's CLK, so the encoder has to move first. ---"},{"u":"device.html","p":"The device","t":"Running on the Pi","a":"running-on-the-pi","x":"from tick.display import bootstrap bootstrap() # run() calls this for you, before pygame is imported It does two things, both of which are the difference between a working panel and a black one: * Sets `SDL_VIDEODRIVER=kmsdrm` when nothing already owns the display (a running Wayland or X11 session is preferred and left alone). * Re-execs under the **system** Python if the active pygame came from pip. The pip wheel has no KMSDRM support, so without this the game opens on nothing. The panel's DRM card index varies by board. Set it for yours rather than trusting a default: SDL_VIDEO_KMSDRM_DEVICE"},{"u":"device.html","p":"The device","t":"Headless","a":"headless","x":"For tests, captures and CI: SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python -m unittest discover -s tests -t . `Harness` needs neither; only `Harness.draw_once()` touches pygame at all."},{"u":"docs-site.html","p":"This site","t":"The docs site","a":"the-docs-site","x":"`web/` turns the Markdown already in this repo into a small static site, so the documentation can be read in a browser with a sidebar, a contents rail and search \u2014 instead of scrolling through raw `.md` files. tick docs # build and open it python web/build.py # build only, into web/dist/ python web/build.py --serve # build, serve on :8000, open a browser open web/dist/index.html # or just open the file; no server needed"},{"u":"docs-site.html","p":"This site","t":"How it works","a":"how-it-works","x":"README.md, PLAN.md, docs/*.md \u2500\u2500\u2510 tick/**.py, examples/*.py, \u251c\u2500\u25ba build.py \u2500\u25ba web/dist/*.html + style.css + app.js tests/*.py \u2500\u2500\u2518 + search-index.js * **The Markdown stays the single source of truth.** Nothing is written twice: edit a `.md`, run the build, and the site follows. There is no separate copy of the docs to drift. * **Every Python file is rendered too.** A link in the prose to `tick/market.py` lands on the actual code with its docstrings intact, which is where a good deal of the reasoning lives. `source.html` lists them all. * **Dead links are dropped, not published.** `build.py` rewr"},{"u":"docs-site.html","p":"This site","t":"Why it has no dependencies","a":"why-it-has-no-dependencies","x":"`mdlite.py` is a small Markdown renderer and syntax highlighter, standard library only. A real Markdown library would parse better, but then reading the docs would require installing something first \u2014 and the whole argument of this SDK is that it runs with what is already on the machine. It handles exactly what these docs use: headings, fenced code, pipe tables, nested and task lists, blockquotes, rules, and inline code, bold, italic and links. The same goes for the page itself: no framework, no bundler, no CDN. `app.js` is about 150 lines of plain JavaScript for search, the contents rail, cop"},{"u":"docs-site.html","p":"This site","t":"Files","a":"files","x":"| Path | What | |---|---| | `build.py` | assembles the site: pages, nav, source rendering, search index | | `mdlite.py` | Markdown \u2192 HTML, and the code highlighter | | `theme/style.css` | the design \u2014 the device's own palette, light and dark | | `theme/app.js` | search, contents rail, copy buttons, theme, mobile nav | | `dist/` | the built site, committed so a checkout can be read immediately |"},{"u":"docs-site.html","p":"This site","t":"Adding a page","a":"adding-a-page","x":"Write the Markdown in `docs/`, then add one row to `PAGES` in `build.py`: ('tuning.html', 'docs/tuning.md', 'Reference', 'Tuning', 'Knobs worth turning.'), The nav group, the previous/next links and the search entries follow from that row. Rebuild and the page is in the site."},{"u":"docs-site.html","p":"This site","t":"Keeping it honest","a":"keeping-it-honest","x":"[`tests/test_web.py`](../tests/test_web.py) builds the site and checks that every page renders, every internal link resolves to a file that exists, every `#anchor` matches a real heading id, and no documented link was silently dropped. Broken documentation links are a slow leak; this closes it."},{"u":"src-tick-__init__.html","p":"Source","t":"tick/__init__.py","a":"","x":"TICK SDK -- build a crypto arcade game for the handheld in one file. from tick import Game, run class MyGame(Game): title = 'MY GAME' window_s = 10.0 def on_action(self, ctx, action): if action.name == 'A': ctx.bet(ctx.price, ctx.price * 1.001) def draw(self, ctx, screen): from tick.hud import clear, draw_hud clear(screen) draw_hud(ctx, screen) run(MyGame()) What you get for those lines: a live price feed (simulated, an exchange, or on-chain pools), a round clock that never stops, odds priced from measured volatility, an integer-USDC wallet that can be paper money or real escrowed funds, a rot"},{"u":"src-tick-bets.html","p":"Source","t":"tick/bets.py","a":"","x":"One bet type, because there is only one bet. Every game on this device -- box on a ladder, over/under, coin flip, hi-lo -- is the same claim wearing different clothes: the price will be between LOW and HIGH when round N rings. A coin flip is `(spot, +inf)`. An over/under is a one-sided range. A box is a two-sided one. So the SDK has exactly one `Bet`, and your game's job is to decide which range the player is pointing at and what it looks like on screen. Topping up ---------- Pressing buy again on a bet already placed **adds stake at the odds available at that moment**, not at the odds of the "},{"u":"src-tick-chain.html","p":"Source","t":"tick/chain.py","a":"","x":"The smallest chain client that can hold a player's money honestly. Plain JSON-RPC over HTTPS with the standard library, plus `eth-account` and `eth-abi` for signing and ABI encoding. No web3, no async, no provider stack: this runs on a Pi Zero and installs in seconds. Everything here is **blocking**. That is deliberate and safe, because nothing here is ever called from the frame loop -- `tick.escrow` runs it on a worker thread and the game only reads finished state. A slow RPC must never cost a frame; a dropped frame on a handheld is felt immediately. The device key -------------- Created on f"},{"u":"src-tick-cli.html","p":"Source","t":"tick/cli.py","a":"","x":"`tick` -- start a game, watch the data, check the machine. tick doctor what is installed, configured and reachable tick new mygame scaffold a game you can run immediately tick run mygame.py start a game file tick prices --source coinbase --limit 20 print the feed tick pools print every pool in the on-chain stream tick record -o eth.ndjson --seconds 120 save a market tick replay eth.ndjson --speed 10 play one back tick board the leaderboard, read from ENS tick docs build the documentation site and open it `prices` and `pools` exist because the first question anyone has is \"is the data actually "},{"u":"src-tick-display.html","p":"Source","t":"tick/display.py","a":"","x":"One canvas, 480x320, landscape, whatever the panel underneath is doing. Your game draws on a 480x320 surface and never thinks about the display again. If the actual panel is portrait -- like the 320x480 Waveshare SPI screen on this handheld -- `present()` turns each finished frame onto it. Touch and mouse positions are turned back the other way, so a tap lands where the player pointed, not where the panel thinks they did. That seam is worth its small cost: a game written against a rotating canvas is a game with rotation maths smeared through its draw code and its input code, and it is wrong in"},{"u":"src-tick-env.html","p":"Source","t":"tick/env.py","a":"","x":"Settings, so a device can be configured without a shell. A handheld has no terminal. Everything tunable therefore lives in a `.env` file next to the game, read once at startup, and **anything already set in the real environment wins** -- so a one-off TICK_MARKET_SOURCE=coinbase python mygame.py still overrides the file. Dependency-free on purpose: no python-dotenv on a Pi. The settings every SDK layer reads ---------------------------------- TICK_MARKET_SOURCE sim | coinbase | substreams | replay | frozen TICK_ASSET eth | btc | sol | hbar TICK_FUNDING demo | arc-testnet TICK_STAKE_USDC money p"},{"u":"src-tick-escrow.html","p":"Source","t":"tick/escrow.py","a":"","x":"Real money, without asking the player to trust the handheld. The flow, and why each step exists ---------------------------------- 1. The device shows **its own address** as a QR code. The player sends USDC from their own wallet -- a plain send from MetaMask works, no dApp, no connect, no signature prompt on a 480x320 screen. 2. The device watches the chain for that transfer, keeps a small fixed gas fee (USDC is the gas token on Arc, so the device never needs a second coin), and locks the rest in `TickEscrow` with `openFor`, **in the sender's name**. 3. Bets run on the device against that bala"},{"u":"src-tick-feeds-__init__.html","p":"Source","t":"tick/feeds/__init__.py","a":"","x":"Feeds: where the number comes from. open_feed('sim') seeded random walk, no network (default) open_feed('coinbase') live public REST ticker, 5 Hz open_feed('substreams') on-chain pools via substreams/relay.py open_feed('frozen') a price that never moves (for tests) open_feed('replay', path='eth.ndjson') a recorded market Pick one with the TICK_MARKET_SOURCE setting and every game in this SDK changes feed without touching a line of game code. That is the whole point of the seam: your game reads `ctx.market.price`, and never knows or cares."},{"u":"src-tick-feeds-base.html","p":"Source","t":"tick/feeds/base.py","a":"","x":"What every feed is, and the two ways to read one. A feed is a source of `PriceTick`. It has exactly three methods: poll(dt, now) -> list[PriceTick] non-blocking; returns what has arrived close() stop threads, drop sockets .name / .status words for the screen `poll` is non-blocking on purpose. A game runs a frame loop at 30 FPS; a feed that blocks waiting for the next block drops frames, and a dropped frame on a handheld is felt immediately. Live feeds therefore do their waiting on a worker thread and hand finished ticks over a queue. For anything that is not a game -- a script, a bot, a termin"},{"u":"src-tick-feeds-coinbase.html","p":"Source","t":"tick/feeds/coinbase.py","a":"","x":"Live trades from a public exchange, with no key and no account. Read-only REST, polled at 5 Hz on a worker thread. Kept dependency-free (no requests, no websockets) so it installs on a Pi with the stock Python. Two decisions worth knowing, because both were bugs first: * **Every successful poll is a tick, even one repeating the last trade.** ETH-USD can go seconds without a trade. Aging the price by its last *trade* made a quiet book look like a dead feed, and bets were refused as stale while the connection was perfectly fine. * **A trade id older than one already seen is dropped, not replayed"},{"u":"src-tick-feeds-replay.html","p":"Source","t":"tick/feeds/replay.py","a":"","x":"Record a market once, replay it forever. Live markets are terrible to develop against: you cannot make the price do the thing you need to see, and you cannot make it do it twice. So record a real feed to a file, then replay it -- at wall speed to watch your game, or as fast as the CPU allows to run a thousand rounds in a test. tick record --source coinbase --seconds 300 -o eth.ndjson tick replay eth.ndjson --speed 4 Recording format is one JSON object per line: {\"t\": 0.0, \"p\": 2500.12, \"a\": 0.4}. `t` is seconds since the recording began, so a file is portable and readable, and `a` is the sourc"},{"u":"src-tick-feeds-sim.html","p":"Source","t":"tick/feeds/sim.py","a":"","x":"A believable market with no network: the feed you develop against. Geometric Brownian motion at the coin's real volatility, seeded so the same seed replays the same market exactly. That is what makes a test of your odds a test and not a coin flip. Tick spacing is held in **integer microseconds**, and each frame's delta is rounded into that accumulator rather than truncated. Accumulating float seconds drifts, and truncating 1/60 of a second loses two thirds of a microsecond every frame -- enough that the same ten seconds of wall clock yields 200 ticks at one frame rate and 199 at another, which"},{"u":"src-tick-feeds-substreams.html","p":"Source","t":"tick/feeds/substreams.py","a":"","x":"On-chain prices: every swap in every watched ETH/USDC pool, block by block. The shape of it --------------- The Graph -> substreams run (one per chain) -> relay.py -> your game JSON lines on stdout HTTP ndjson PriceTick The relay runs on a laptop or a server, because the Substreams key must not live on a handheld somebody can pick up. The device holds one long-lived HTTP connection to `GET /stream` and reads newline-delimited JSON: one line per block per chain. Each line looks like {\"chain\": \"arbitrum\", \"@block\": 21, \"@data\": { \"blockNumber\": 3103, \"timestampMs\": 1757000000000, \"prices\": [{\"po"},{"u":"src-tick-game.html","p":"Source","t":"tick/game.py","a":"","x":"What you write, and what you are handed. A game is a class with a few optional methods. Every one of them is optional except `draw`; the SDK calls what exists and skips what does not. from tick import Game, run class Coinflip(Game): title = 'COINFLIP' window_s = 10.0 def setup(self, ctx): self.pick = 'UP' def on_action(self, ctx, action): if action.name in ('UP', 'DOWN'): self.pick = action.name if action.name == 'A': low, high = (ctx.price, 1e12) if self.pick == 'UP' else (0.0, ctx.price) ctx.bet(low, high) def draw(self, ctx, screen): ... run(Coinflip()) Everything else -- the feed, the roun"},{"u":"src-tick-hud.html","p":"Source","t":"tick/hud.py","a":"","x":"A screen that already looks like the device, so your game can be one file. Three pieces, all optional: * `draw_hud(ctx, screen)` -- the top bar (game, price, balance) and the bottom bar (button hints and the status line). Two calls and your game is legible. * `Ladder` -- price to pixels and back. Every game here is a claim about where a number will be, so every game needs a vertical price scale; this is it. * `draw_bet` / `draw_cursor` -- the SDK's drawing convention for bets, so a player who has used one of these games can read the next one: **dashed means not committed, solid means money is "},{"u":"src-tick-identity.html","p":"Source","t":"tick/identity.py","a":"","x":"Players have names, and the names carry their record. A wallet address is not an identity a person can read, remember, or be proud of. `0x7ee8...CCac` on a leaderboard is noise. So the SDK gives every wallet that plays a **subname under a parent ENS name** -- `amber-otter.tick.eth` -- and writes that player's record into the name's own text records. Why the record lives on the name -------------------------------- The alternative is a database, which means a server, which means your game's leaderboard dies when you stop paying for it and cannot be read by anything else. Text records on an ENS "},{"u":"src-tick-input.html","p":"Source","t":"tick/input.py","a":"","x":"Four actions. That is the whole controller. UP / DOWN the dial, one detent A go / buy / confirm B back / cancel QUIT leave A rotary encoder, two panel buttons, a keyboard and a touchscreen all collapse into those four, so a game written on a laptop runs on the handheld with no input code changed. That is the only reason a game is testable at all: you cannot unit-test a thumb on a dial, but you can feed a list of `InputAction`. Hardware (BCM numbering, not header position -- this is where it usually goes wrong): encoder CLK=GPIO21 (pin 40) DT=GPIO20 (pin 38) SW=GPIO16 (pin 36) red GPIO13 (pin 3"},{"u":"src-tick-market.html","p":"Source","t":"tick/market.py","a":"","x":"The feed, plus everything a game needs to know about it before betting. A raw feed hands you prices. A `Market` answers the four questions that decide whether a bet may be sold at all: market.fresh(now) is the last price recent enough to settle money on? market.ready has the feed moved enough to measure anything yet? market.quiet(now) has it stopped moving, so every bet is a certainty? market.variance how much is it actually moving, right now? and one question that decides the price of the bet: market.probability(low, high, horizon) Measuring volatility: read this before changing it ----------"},{"u":"src-tick-money.html","p":"Source","t":"tick/money.py","a":"","x":"Money, in integers, because floats lose it. Everything here is **micro-USDC**: whole units of 0.000001 USDC, the real on-chain unit. A balance that is an int cannot drift the way repeated float addition does, and `0.1 + 0.2` never has to be explained to a player. Only mark-to-market maths runs in floats, and it is truncated back to an int the moment it becomes a balance -- truncated, never rounded, so a payout can never round money into existence that the book did not owe. The funding seam ---------------- `Wallet` holds the balance. *Where the balance comes from* is a separate object with thr"},{"u":"src-tick-pricing.html","p":"Source","t":"tick/pricing.py","a":"","x":"Odds, from first principles, in about fifty lines. The question every game on this device asks is the same one: the price is P now. What is the chance it is between LOW and HIGH in H seconds -- and what multiple should I pay someone who calls it? **Chance.** Over a short horizon a price is a random walk in log space. The distance it travels grows with the *square root* of time, not with time. So one standard deviation over H seconds is `P * sqrt(variance_per_second * H)`, and the chance of landing in a range is the normal CDF at the top minus the CDF at the bottom. That is `probability()` belo"},{"u":"src-tick-rounds.html","p":"Source","t":"tick/rounds.py","a":"","x":"The clock that never stops. A round is a fixed slice of time with a price at each end. Rounds follow each other with no gap and no button: when one ends the next has already started, in the same frame. There is no \"ready?\" screen, because a clock you can pause is a clock you can wait out, and waiting out the market is not a game. round 7 round 8 round 9 |----------------|----------------|----------------| open bell/open bell/open bell 2500.00 2500.41 2499.88 Each round records the price it **opened** at. Draw movement against that, not against spot: anchoring a chart on spot re-centres it ever"},{"u":"src-tick-runtime.html","p":"Source","t":"tick/runtime.py","a":"","x":"The loop you do not have to write. `run(MyGame())` starts everything in the right order and shuts it down in the reverse one: .env -> SDL bootstrap -> display -> feed -> wallet/escrow -> round clock -> sound -> your setup() -> 30 FPS loop -> teardown -> close it all Inside each frame, in this order, and the order matters: 1. read the feed, fold every tick into the market 2. start the clock on the first price; roll it if a bell has passed 3. settle the bets on any round that closed, and pay out 4. play the generic cues (bell, crossings, countdown, win/miss, stale) 5. hand input to your game 6. "},{"u":"src-tick-templates-game.html","p":"Source","t":"tick/templates/game.py","a":"","x":"GAME_TITLE -- a TICK game. Run it: python this_file.py Live prices: TICK_MARKET_SOURCE=coinbase python this_file.py Real money: TICK_FUNDING=arc-testnet python this_file.py Controls: dial or Up/Down moves the aim, A (Enter/Space/yellow) buys, B (Backspace/red) quits, Escape quits."},{"u":"src-tick-testing.html","p":"Source","t":"tick/testing.py","a":"","x":"Play a thousand rounds in a second, with no window and no waiting. A game that settles money has rules -- what it refuses to sell, what it pays, what it does when the feed dies -- and rules that are only ever eyeballed are rules that are wrong. The harness here runs your real game class against a real market and a real wallet, on a clock you control, with no display. from tick.testing import Harness def test_a_box_on_spot_pays_about_two(): h = Harness(MyGame(), source='sim', seed=7) h.advance(30) # 30 seconds of market bet = h.ctx.bet(h.ctx.price - 1, h.ctx.price + 1) assert 1.5 < bet.multiple"},{"u":"src-tick-ticks.html","p":"Source","t":"tick/ticks.py","a":"","x":"The one unit every layer above passes around: a price, stamped and numbered. A tick is not a float. A float cannot tell you *when* it was true, whether it arrived out of order, or how long it sat in a relay before it reached you -- and a game that settles money on a stale number is broken in a way no amount of UI polish fixes. So every feed in this SDK produces `PriceTick`, and nothing downstream accepts a bare number."},{"u":"src-tick-ui.html","p":"Source","t":"tick/ui.py","a":"","x":"The look and the noise: a 480x320 pixel kit you do not have to design. Two things live here. A **palette and a handful of drawing helpers** so a game written in an afternoon still looks like it belongs on this device. And **`Sounds`**, which synthesizes every cue and every music bed at startup from square waves -- no audio assets, no dependencies, about 0.16s to build. Why synthesized: a handheld you play with your thumb has to tell you things your eyes are too busy to read. The two that matter most are the swoops when the price crosses into or out of your bet, and the countdown ticks pitched "},{"u":"src-examples-coinflip.html","p":"Source","t":"examples/coinflip.py","a":"","x":"COINFLIP -- the smallest real game on this SDK. Up or down at the bell. Run: python examples/coinflip.py TICK_MARKET_SOURCE=coinbase python examples/coinflip.py There is no aim, no geometry and no chart: pick a side, press A, and the round settles on which side of the opening price the market closed. It exists to show how little a game has to do -- the feed, the clock, the odds, the wallet and the payout are all the SDK's. Note what the odds do. A coin flip on a price is very close to 50/50, so the quote sits a hair under 2x and *moves with the market*: when volatility drops the two sides stop"},{"u":"src-examples-ladder.html","p":"Source","t":"examples/ladder.py","a":"","x":"LADDER -- park a box on the price and be inside it at the bell. Run: python examples/ladder.py This is the shape most games on this device end up being, so it is worth reading as the reference: * The box is **always the same size**, fixed in basis points of the round's opening price. It never changes under a bet already placed. Sizing it in volatility instead means the market redraws a box you already bought, which is both confusing and dishonest about what you paid for. * **Only the payout reacts to the market.** A wild market makes the same box genuinely harder to hit, and the multiple rises"},{"u":"src-examples-pools.html","p":"Source","t":"examples/pools.py","a":"","x":"Every watched pool, block by block, straight off the on-chain stream. python examples/pools.py [relay-url] One line per block per chain, then one per pool that priced in it. This is the raw material the combined price is made of: use it for a dashboard, a spread monitor, or a game that plays two venues against each other rather than flattening them into one number. Needs the relay running (see docs/prices-substreams.md)."},{"u":"src-examples-ticker.html","p":"Source","t":"examples/ticker.py","a":"","x":"The data layer with no game attached: prices in a terminal. python examples/ticker.py simulated python examples/ticker.py coinbase live exchange python examples/ticker.py substreams on-chain pools via the relay Proof that the SDK is a data library first and a game engine second. There is no pygame import anywhere in this file."},{"u":"src-tests-__init__.html","p":"Source","t":"tests/__init__.py","a":"","x":""},{"u":"src-tests-test_bets.html","p":"Source","t":"tests/test_bets.py","a":"","x":"Placing, topping up, settling, refunding -- every path money takes."},{"u":"src-tests-test_cli.html","p":"Source","t":"tests/test_cli.py","a":"","x":"The command line, and the settings file it reads."},{"u":"src-tests-test_escrow.html","p":"Source","t":"tests/test_escrow.py","a":"","x":"The real-money state machine, with a fake chain: no network, no keys. This is the point of keeping every on-chain call behind `Chain`. The rules that decide whether a player's deposit becomes a session, and whether their winnings come back, are testable on a laptop in milliseconds."},{"u":"src-tests-test_examples.html","p":"Source","t":"tests/test_examples.py","a":"","x":"Every shipped example runs headless and draws a frame without raising. Run with a dummy display, as CI and the Makefile do: SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python -m unittest discover -s tests -t ."},{"u":"src-tests-test_feeds.html","p":"Source","t":"tests/test_feeds.py","a":"","x":"Feeds: determinism, pool combination, recording round trips."},{"u":"src-tests-test_game.html","p":"Source","t":"tests/test_game.py","a":"","x":"The whole machine, headless: a real game class against a real market."},{"u":"src-tests-test_identity.html","p":"Source","t":"tests/test_identity.py","a":"","x":"Names, without touching a network: the parts that are pure functions."},{"u":"src-tests-test_market.html","p":"Source","t":"tests/test_market.py","a":"","x":"What the market refuses to price, and why."},{"u":"src-tests-test_money.html","p":"Source","t":"tests/test_money.py","a":"","x":"Money is integers. These tests are why."},{"u":"src-tests-test_pricing.html","p":"Source","t":"tests/test_pricing.py","a":"","x":"The odds maths, on its own: no feed, no wallet, no game."},{"u":"src-tests-test_rounds.html","p":"Source","t":"tests/test_rounds.py","a":"","x":"The clock: it rolls on its own, and it will not settle on the wrong price."},{"u":"src-tests-test_web.html","p":"Source","t":"tests/test_web.py","a":"","x":"The docs site builds, and nothing in it points at nothing. Broken links in documentation are a slow leak: each one costs a reader a click to discover that the thing they wanted is not there. These tests are cheap and they close it."}];
